/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package my_first_project.app;

import my_first_project.list.LinkedList;

import static my_first_project.utilities.StringUtils.join;
import static my_first_project.utilities.StringUtils.split;
import static my_first_project.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
